import type { Metadata } from "next";
import { Geist } from "next/font/google";
import "./globals.css";
import Header from "@/components/Header";
import Footer from "@/components/Footer";

const geist = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Double Click Computing | IT Services NJ",
  description:
    "Professional IT support for businesses and homes across New Jersey. 38 years of experience. Managed IT, cybersecurity, networking, and on-site support.",
  keywords:
    "IT support New Jersey, managed IT services NJ, computer repair NJ, business IT support, home computer help NJ, cybersecurity NJ",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${geist.variable} h-full antialiased`}>
      <body className="min-h-full flex flex-col">
        <Header />
        <main className="flex-1">{children}</main>
        <Footer />
      </body>
    </html>
  );
}
